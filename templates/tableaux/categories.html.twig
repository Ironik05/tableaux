{% extends 'base.html.twig' %}

{% block title %}Nos Catégories - L'Art Nous Protège{% endblock %}

{% block body %}
<div class="container mt-5">
		<h1 class="mb-4">🛍️ Nos Rayons</h1>

		<div class="row">
			{% for category in categories %}
				<div class="col-md-4 mb-4">
					<div class="card h-100 shadow-sm">
						<div class="card-body">
							<h5 class="card-title">{{ category.name}}</h5>
							<p class="card-text text-muted">{{ category.produits|length }}
								produit(s)</p>
						</div>
						<div class="card-footer bg-white">
							<a href="{{ path('app_tableaux_par_categorie', {id: category.id}) }}" class="btn btn-primary btn-sm w-100">
								Voir les produits
							</a>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	</div>
{% endblock %}